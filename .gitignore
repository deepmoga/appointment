# Installer artifacts
.installed
*.zip

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo

# PHP local config overrides — never commit live credentials
includes/config.php

# Uploads directory (if you add one later)
uploads/

# Logs
*.log

# Auto-deploy webhook (server-only, contains secret)
deploy.php
